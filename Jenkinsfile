pipeline {
    agent any
    stages {
        stage('Deployment Repo') { 
            steps {
                script {
                    sh "echo 'Deployment done using latest repo'"
                }
            }
        }
    }
}
